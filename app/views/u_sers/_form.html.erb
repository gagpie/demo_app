<%= form_for(@u_ser) do |f| %>
  <% if @u_ser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@u_ser.errors.count, "error") %> prohibited this u_ser from being saved:</h2>

      <ul>
      <% @u_ser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
